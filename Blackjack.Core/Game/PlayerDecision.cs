namespace Blackjack.Core.Game
{
    // Represents the set of actions a player (human or bot) can choose from.
    public enum PlayerDecision
    {
        Hit = 1,
        Stand = 2,
        DoubleDown = 3,
        Split = 4
    }
}
